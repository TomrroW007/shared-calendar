version: '3.8'

services:
  shared-calendar:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - calendar-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production

volumes:
  calendar-data:
